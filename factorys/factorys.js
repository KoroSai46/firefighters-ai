const FireStationFactory = require('./FireStation');
const BotFactory = require('./Bot');
const WildFireFactory = require('./WildFire');

module.exports = {
    FireStationFactory,
    BotFactory,
    WildFireFactory
}